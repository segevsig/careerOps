# Copy to .env and set values as needed.
# Docker Compose loads .env from the project root.
# AI_SERVICE_URL is used by backend/worker to call the local AI service (cover letter + resume scoring).

AI_SERVICE_URL=http://localhost:8000
